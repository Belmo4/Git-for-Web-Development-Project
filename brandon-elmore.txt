https://codepen.io/Belmo4/pen/GRRMZVP

. What is Semantic HTML? Gives your html meaning.
    2. What is HTML used for? HTML is used for building content etc on webpages.
    3. What is an attribute and where do we put it?  Gives additional information to an element.
    4. What is the h1 tag used for? How many times should I use it on a page? It is the mean heading of page and only once.
    5. Name two tags that have required attributes. Img and a tag.
    6. What do we put in the head of our HTML document? Metadata
    7. What is an id? An id gives an element a unique id.
    8. What elements can I add an id to? Any
    9. How many times can I use the same id on a page? Once
    10. What is a class? An attribute given to elements.
    11. What elements can I add a class to? Any
    12. How many times can I use the same class on a page? Infinite
    13. How do I get my link to open in a new tab? blank_"
    14. What is the alt attribute used for? Give images an alternate text.
    15. How do I reference an id? #id
    16. What is the difference between a section and a div? Div divides content while section groups it.
    17. What is CSS used for? Styling HTML
    18. How to we select an element? Example - every h2 on the page h2{}
    19. What is the difference between a class and an id? - Classes can be used on multiple elements while id can only be used on one.
    20. How do we select classes in CSS? .class
    21. How do we select a p element with a single class of “human””? p.human
    22. What is a parent child selector?  Happens when the element is a direct descendent of a parent element. Making up at least 2 selectors.
    23. How do you select all links within a div with the class of sidebar? .sidebar
    24. What is a pseudo selector? selector that selects elements that are in a specific state
    25. What do we use the change the spacing between lines? line-height:
    26. What do we use to change the spacing between letters? letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform:
    28. How do I add a 1px border around my div that is dotted and black? border: 1px dotted black;
    29. How do I select everything on the page? *{}
    30. How do I write a comment in CSS? */ /*
    31. How do I find out what file I am in, when I am using the command line? cd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?  git rm -r You can not undo this command.
    34. Why should I use version control? It is easy to collaborate and see changes made.
    35. How often should I commit to github? 20~ min
    36. What is the command we would use to push our repo up to github? git push
    37. Walk me through Lambda's git flow. 
    Get own version by forking, Add TL as contr, Clone repo, Create branch.

Stretch Questions

    1. What is the difference between an inline element and a block element? Inline elements do not create a new line while block elements cause a line break to happen.
    2. What happens when an element is positioned absolutely? It is taken out of the flow and other elements ignore it.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? Give the div overflow:hidden
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model?  Box model is used for design that enables stuff to be wrapped into margins, borders, padding, and the content.
